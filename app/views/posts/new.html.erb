<h1>Create a new post</h1>

<%= form_for :post, url:posts_path do |f| -%>
  <%= f.label :title, "Title" %>
  <%= f.text_field :title %>
  <%= f.label :body, "Body" %>
  <%= f.text_area :body %>
  <%= f.submit "Create", :disable_with => 'Submiting...' %>
<% end %>